const sum = (a, b) => a + b;

const sub = (num1, num2) => num1 - num2;

const mut = (n1, n2) => n1 * n2;

const media = ([n1, n2]) => (n1 + n2) / 2;

module.exports = {sum, sub, mut, media} // exporta uma function, ou elemnto;